$(function() {
	<%	str = '' %>
	<% count = 0 %>
	var strings = []
	bootbox.alert(" ", function() { console.log("Got here"); });
	// $(".bootbox-body").css("overflow-y: auto;");
	
	<% @album.each_with_index do |media_item, index| %>
		$(".modal-body").append("<img src='" + '<%= media_item.images.thumbnail.url  %>' + "' height='75px' width='75px'>")
		<% if index > 100 then break end %>
		// <%	str << "<div style='float:left;'>" %>
		// <%	str << "<img src=#{media_item.images.thumbnail.url}><br/>" %>
		// <%	str << "<a href='/media_like/#{media_item.id}'>Like</a>" %>
		// <%	str << "<a href='/media_unlike/#{media_item.id}'>Un-Like</a>" %>
		// <%	str << "<br/>LikesCount=#{media_item.likes[:count]}</div>" %>
	<% end %>
});

